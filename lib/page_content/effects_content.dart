import 'package:flutter/material.dart';

class EffectsContent extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
        child: Text("Effects", style: Theme.of(context).textTheme.titleLarge)
    );
  }
}
